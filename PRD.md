# PRD — 자막 생성기 소통 창구 에이전트

- 작성: 2026-09-17
- 과제: 모두의연구소 「에이전트 팀 꾸리기_Agt1」 5강 [실습 프로젝트] 라우팅 에이전트 만들기
- 바탕: 전날 실습 `modumall-agent`(모두몰 고객응대 에이전트)의 구조
- 대상 프로그램: `subtitle-tc-generator`(자막 및 TC 생성기)

## 1. 무엇을 만드나

- `subtitle-tc-generator` 사용자의 **문의·건의를 받는 창구**
- 문의 한 줄 → 카테고리 판정 → 카테고리별 근거만 조립 → 근거로만 답변 → 검증
- 근거에 없으면 지어내지 않고 **넘긴다**
  - 한국어 어문 규범 질문: 국립국어원 안내
  - 건의·버그: 로컬 접수 기록

## 2. 누구를 위해

- 자막 작업자(TC·번역·SDH) — 이 프로그램 사용자
- 필요한 것
  - 사용법 질문에 README를 뒤지지 않고 답을 얻는다
  - 검사 결과(위반 항목)가 왜 떴는지, 어떻게 고치는지 조항 근거로 안다
  - 자막 규정 질문에 조항 근거로 답을 얻는다
  - 건의가 사라지지 않고 기록된다

## 3. 왜

- 사용법·규정 질문은 반복된다. 매번 문서를 찾는 비용이 크다
- 맞춤법·규정은 **문서를 안 읽어도 그럴듯하게 답하기 쉬운** 분야다
  - 예: 읽기 속도 상한은 번역 자막(성인 12자/초)과 SDH(성인 14자/초)가 다르다. 문서를 안 읽으면 한 숫자로 뭉뚱그려 답한다
  - 예: 넷플릭스 General Requirements에서 프레임 간격 절이 빠진 것(2020-07-24)을 "규정 삭제"로 오해하기 쉽다. 실제로는 Subtitle Timing Guidelines로 옮겨져 최소 2프레임이 살아 있다
  - 틀린 규정 안내는 납품 반려로 이어진다
- 그래서 "근거 없으면 넘긴다"가 이 창구의 핵심 기능이다

## 4. 카테고리 (초안)

| # | 카테고리 | 예시 문의 | 근거 / 처리 |
|---|---|---|---|
| 1 | `USAGE` 사용법 | "원본 타임코드는 그대로 두고 번역만 하려면?" | 생성기 README·사용자용 문서 |
| 2 | `QC_EXPLAIN` 검사 결과 설명 | "읽기 속도 초과가 떴는데 어떻게 고쳐요?" | 검사 항목 목록 + 연결된 규정 조항 |
| 3 | `STYLE_RULE` 자막 규정 | "SDH 화자명은 어떻게 표기해요?" | 넷플릭스 공개 한국어 스타일 가이드 |
| 4 | `FEEDBACK` 건의·버그 | "VTT 형식도 지원해 주세요" | 접수 도구 → 로컬 JSON 기록, 접수번호 반환 |
| 5 | `OUT_OF_SCOPE` 범위 밖·넘기기 | "'되요'가 맞나요 '돼요'가 맞나요?" | 국립국어원 안내(온라인가나다 등) |

- 나눈 근거: **처리 방식이 다르다** — 문서 조회(1·2·3) / 기록(4) / 넘기기(5)
- 1·2·3은 근거 문서가 서로 겹치지 않게 나눈다
- 판정 순서 원칙
  - 어문 규범 질문이라도 3번 근거에 조항이 있으면 3번으로 답한다
  - 조항이 없으면 5번으로 넘긴다
- 카테고리↔문서 조항 매핑표는 문서 수집 뒤 `docs/MAPPING.md`로 확정

## 5. 근거 문서 — 공개 가능한 것만

| 문서 | 출처 | 공개 저장소에 |
|---|---|---|
| 생성기 사용법 | `subtitle-tc-generator` README 등 | 필요한 부분 발췌 |
| 검사 항목 설명 | 생성기 `checker/` 검사 목록 | 항목명·설명만 정리 |
| 넷플릭스 한국어 Timed Text Style Guide | partnerhelp.netflixstudios.com (공개 웹) | 조항 요약 + 원문 링크 |
| 넷플릭스 Timed Text Style Guide — General Requirements | 같음 | 같음 |
| 넷플릭스 Timed Text Style Guide — Subtitle Timing Guidelines | 같음 | 같음 |

- **넣지 않는 것**
  - `subtitle-tc-rules` 비공개 저장소 내용
  - 작업자 자료(구글독스 5건)
  - 디즈니+·쿠팡플레이 규정(비공개 자료)
- 넷플릭스 가이드 원문 통째 복제 여부는 수집 단계에서 이용 조건 확인 후 결정

## 6. 평가셋

- 목표: **40건 이상**, 카테고리별 비슷하게
  - 과제 최소 10건. 모두몰은 eval 32건이었다
- 문항마다 적는 것
  - `expected_route`: 기대 카테고리
  - `expected_tools`: 기대 도구 집합(넘기기 포함)
  - `must`: 반드시 담을 사실
  - `forbid`: 문서를 안 읽으면 나오기 쉬운 그럴듯한 오답
- 넘기기가 정답인 문항을 반드시 섞는다
- 분할: `fewshot`(프롬프트 예시용) / `eval`(채점용) — eval은 프롬프트에 절대 안 넣는다
- 문항 작성 자료 (2026-09-17 변경 — 온라인가나다는 자막 작업자 문의 분포와 맞지 않아 제외)
  - 번역자·영상 작업자가 쓰는 사이트의 Q&A — **문의 유형·말투만 참고해 새로 쓴다.** 원문 복제·자동 수집 안 함
  - 확정 출처 (2026-09-17 사용자 선택)
    - Subtitle Edit GitHub 이슈·토론 (영어) — 사용법·검사 결과·건의 질문 유형
    - 루리웹·클리앙 등 한국어 커뮤니티 — 한국어 말투, 범위 밖 질문(작업료·분업 등)
  - 영어 질문은 한국어 작업자 말투로 바꿔 쓴다
  - 생성기 사용 중 실제로 나올 법한 질문
- 국립국어원은 문항 자료가 아니라 **넘기기 안내 대상**으로만 쓴다

## 7. 측정

| 지표 | 정의 |
|---|---|
| 분류 정확도 · macro F1 · 혼동 행렬 | 루브릭 요구 |
| 도구 호출 적절성 | 실제 호출 도구 집합 == 기대 도구 집합이면 1점 |
| 답변 적절성 | `must` 전부 포함 + `forbid` 0건이면 1점. 표현 아닌 사실로 판정 |

- 채점기는 **모범 답안으로 먼저 검증**한다
  - 모두몰 교훈: 원본 채점기가 모범 답안 2건을 통과시키지 못했다
- 틀린 건은 직접 읽고 원인을 분류해 기록한다
- 개선은 한 번에 하나씩, 매번 재고, `runs/LAB.md`에 기록한다

## 8. 기술 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| LLM | OpenAI | 전날 실습 코드 재사용 |
| 파이프라인 | LangGraph | 과제 권장, 전날 코드와 같음 |
| 데모 | Streamlit 또는 Gradio | 구현 단계에서 결정 |
| 저장소 | 새 공개 저장소 `subtitle-help-agent` | 제출용. 생성기 저장소와 분리 |

- **외부 전송 범위**: 사용자 문의 문장과 공개 문서 발췌만 OpenAI로 간다
  - 미공개 영상·대본은 이 창구의 입력이 아니다(생성기 규칙 6과 충돌 없음)
  - 데모 화면에 "대본·미공개 자막을 붙여넣지 마세요" 안내를 넣는다
- API 키는 `.env`로만. 저장소에 올리지 않는다

## 9. 전날 코드에서 가져올 것

| 모듈 | 가져오는 이유 |
|---|---|
| `router.py` | 분류와 확신도 기반 넘기기(`gate`)가 이미 분리돼 있다 |
| `guardrail.py` | 답변 속 숫자의 출처 역추적 — 규정 답변은 숫자(글자 수·ms)가 핵심 |
| `context.py` | 문서 쪼개기·라우트별 근거 조립 |
| `evaluate.py` · `evaluate_fixed.py` | 지표 측정·채점기 검증 |

- 전날 교훈 중 그대로 가져갈 원칙
  - 점수를 올린 변경은 대부분 "프롬프트가 문서를 요약하며 빠뜨린 조항을 되돌린 것"이었다
  - 단일 측정으로 판정하지 않는다. 흔들림이 있으면 두 번 이상 잰다

## 10. MVP — 가장 작은, 끝까지 도는 조각

- [x] 카테고리 5개 확정 + 매핑표 (`docs/MAPPING.md`)
- [x] 근거 문서 수집·조항 단위 분할 (`docs/`, 조각 110개)
- [x] 평가셋 51건 — fewshot 10 / eval 41 (`data/goldenset.json`)
- [ ] 파이프라인: 판정 → 넘기기 게이트 → 근거 조립 → 답변(도구 호출) → 검증
- [ ] `FEEDBACK` 접수 도구 — 로컬 JSON 기록
- [ ] `evaluate.py` — 4개 지표 + 채점기 검증 + 실패 목록 출력
- [ ] 기준선 측정 1회 + 개선 실험 기록
- [ ] 데모 화면 — 답변·호출 도구·근거 조항·검증 결과 표시
- [ ] `REPORT.md` 7개 항목
- [ ] README(설치·실행법)

## 11. 보류 목록 — MVP 뒤로

- 디즈니+·쿠팡플레이 규정 문의 — 공개 근거 문서가 없다
- 영어 SDH·타 언어 번역 규정
- GitHub 이슈 초안 자동 생성
- 생성기 앱(PySide6) 안에 창구 내장
- 멀티턴 되묻기(전날 `answer_goldenset_multiturn`에 해당)
- 공개 배포

## 12. 성공 기준

| 기준 | 목표 |
|---|---|
| 엔드투엔드 실행 | 데모에서 문의 입력부터 답변·검증 표시까지 오류 없음 |
| 넘기기 문항 | `forbid` 위반 **0건** — 지어낸 답 없음 |
| 도구 호출 적절성 | 기준선 대비 개선을 실험 기록으로 증명 |
| 답변 적절성 | 같음 |
| 채점기 검증 | 모범 답안 통과율 100%, 아니면 통과 못 하는 이유 기록 |
| 제출물 | 루브릭 3항목 각각에 대응하는 근거가 REPORT.md에 있음 |

## 13. 열린 질문

- 넷플릭스 가이드 원문을 저장소에 얼마나 실을지(요약+링크 vs 원문)
- `QC_EXPLAIN`의 입력 형태 — 위반 문구를 붙여넣는가, 자연어로 묻는가
- 데모 도구: Streamlit vs Gradio
