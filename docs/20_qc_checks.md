# 검사 결과 설명 — 넷플릭스 한국어 프로파일 검사 항목

- 카테고리: `QC_EXPLAIN`
- 근거: `subtitle-tc-generator`의 넷플릭스 규정 프로파일(`common` / `ko-translation` / `ko-sdh`)과 검사 구현 목록 (2026-09-17 확인)
- 규정 내용은 `docs/30_style_netflix.md`의 조항 요약을 함께 인용한다
- 섹션 머리의 `[Q-T01]` 같은 표지를 답변에 인용한다

## [Q-00] 검사 결과 읽는 법과 이 창구가 설명하는 범위

- 리포트 표시
  - `[자동]`: 기계가 고칠 수 있다. `--fix`로 새 파일에 고쳐 준다(원본은 그대로)
  - `[확인]`: 사람이 판단한다. 기계가 고치지 않는다
  - `미구현 검사`: 프로파일에는 있으나 아직 검사하지 않는다. **통과가 아니다** — 사람이 직접 봐야 한다
- 이 창구가 설명하는 것: **넷플릭스 공식 가이드에 근거한 검사 항목**(아래 표)
- 이 창구가 설명하지 않는 것
  - 디즈니+·쿠팡플레이 프로파일 항목 — 근거가 비공개 실무 자료다
  - 넷플릭스 프로파일 안의 실무 자료 항목(`C11`·`C12`·`C30`~`C33`) — 같은 이유
  - 위 항목을 물으면 근거를 지어내지 말고, 비공개 자료라 이 창구에서 근거를 안내할 수 없다고 답한다
- 규칙 번호 앞자리
  - `C`: 넷플릭스 공통(번역·SDH 모두)
  - `T`: 한국어 번역 자막(Korean TTSG Section I)
  - `S`: 한국어 SDH(Korean TTSG Section II)

## [Q-C01] 노출 시간 범위 — `duration_out_of_range`

- 근거: `GR-1`, `TM-3`
- 판정: `[확인]`
- 뜻: 자막 하나가 화면에 떠 있는 시간이 최소보다 짧거나 최대(7초)보다 길다
- 고치는 법
  - 짧으면: 아웃점을 늘린다. 다음 자막과 붙어 못 늘리면 앞뒤 자막과 합친다(`TM-6`)
  - 길면: 자막을 둘로 나누거나 아웃점을 당긴다
  - 영상 없이 한꺼번에 맞추려면 `--fix-timing --fix` (`U-07`). 단 `--lock-timecodes`와는 함께 못 쓴다

## [Q-C02] 줄 수 초과 — `too_many_lines`

- 근거: `GR-4`, `KO-I.11`, `KO-II.4`
- 판정: `[확인]`
- 뜻: 한 자막이 2줄을 넘는다
- 고치는 법: 문장을 줄이거나 자막을 나눈다. 번역 자막은 보통 한 줄이 기본이다

## [Q-C03] 통화 환산 — `currency_converted`

- 근거: `GR-9`, `KO-I.12`
- 상태: **미구현 검사**
- 뜻: 대사 속 금액을 원화 등으로 환산했는지 본다. 원래 통화로 둬야 한다

## [Q-C04] 글리프 목록 밖 글자 — `glyph_not_allowed`

- 근거: `GR-2`
- 상태: **미구현 검사**
- 뜻: 넷플릭스 글리프 목록에 없는 글자를 썼는지 본다

## [Q-C05] 자막 사이 간격 — `gap_too_short`

- 근거: `TM-5`
- 판정: `[확인]`
- 뜻: 앞 자막이 끝나고 다음 자막이 시작하기까지 간격이 최소 간격보다 짧거나 두 자막이 겹친다
- 규정: 최소 2프레임. 24fps에서 3~11프레임 간격은 2프레임으로 닫는다
- 프레임 수는 영상 프레임레이트에 따라 밀리초가 달라진다(2프레임 = 23.976fps에서 약 83ms, 29.97fps에서 약 67ms)
- 고치는 법: 앞 자막 아웃점을 늘려 간격을 닫거나, 간격을 반 초 이상으로 벌린다
- 구현 범위: 2프레임 미만 간격·겹침만 잡는다. **3~11프레임 간격 닫기는 검사하지 않는다(미구현)** — 직접 확인한다

## [Q-C10] 화면 글자와 위치 충돌 — `position_collides_with_forced_narrative`

- 근거: `GR-5`
- 판정: `[자동]`
- 뜻: 대사 자막이 화면 글자 자리와 겹친다
- 처리: `--collision` 값을 따른다 — `move_dialogue`(대사 자막을 옮긴다) / `dialogue_only`(대사 자막만 남긴다) / `keep_both`(둘 다 둔다). 위아래 모두 글자가 있으면 읽기 쉬운 쪽에 둔다(`GR-5`)
- 주의: 겹침 처리 방식은 작업마다 달라 `--fn-marker`·`--collision`을 줘야 검사한다(`U-16`)

## [Q-T01] 한 줄 글자 수 초과 (번역) — `line_length_exceeded`

- 근거: `KO-I.2`
- 판정: `[확인]`
- 뜻: 한 줄이 16자를 넘는다. 라틴 문자·공백·문장부호는 0.5자로 센다
- 고치는 법: 문장을 줄이거나 두 줄로 나눈다(최대 2줄). 줄바꿈은 아래 줄이 긴 모양이 좋다(`KO-I.11`)
- 기계가 고치지 않는 이유: 어디를 줄일지는 뜻을 아는 사람이 정해야 한다

## [Q-T02] 읽기 속도 초과 (번역) — `reading_speed_exceeded`

- 근거: `KO-I.15`
- 판정: `[확인]`
- 뜻: 초당 글자 수가 성인 12자, 어린이 9자를 넘는다
- 고치는 법
  - 문장을 줄인다
  - 노출 시간을 늘린다(아웃점 연장, 앞뒤 자막과 합쳐 시간 빌려 오기 `TM-6`)
  - `--lock-timecodes`로 작업 중이면 타임코드는 못 건드리므로 문장만 줄인다(`U-07`)

## [Q-T05] 줄 끝 마침표·쉼표 — `line_end_period_or_comma`

- 근거: `KO-I.13`
- 판정: `[자동]`
- 뜻: 자막 줄 끝에 마침표나 쉼표가 있다
- 고치는 법: 지운다. 한 자막에서 두 문장을 가를 때는 줄 안에서 쉼표를 쓴다

## [Q-T06] 점 세 개 말줄임표 — `three_dot_ellipsis`

- 근거: `KO-I.4`
- 판정: `[자동]` (SDH는 `S15`로 같은 검사)
- 뜻: `...`(점 세 개)를 썼다. 한 글자짜리 `…`(U+2026)로 바꾼다

## [Q-T07] 기울임꼴 — `italics_present`

- 근거: `KO-I.10`
- 판정: `[자동]` (SDH는 `S14`로 같은 검사)
- 뜻: 기울임꼴 태그가 있다. 한국어 자막은 기울임꼴을 쓰지 않으므로 태그를 뺀다

## [Q-T09] 2인 화자 표시 형식 — `dual_speaker_marker_format`

- 근거: `KO-I.6`
- 판정: `[자동]`
- 뜻: 2인 화자 줄 앞 표시가 `하이픈 + 공백` 형식이 아니다
- 고치는 법: 각 줄 앞을 `- `로 맞춘다. 한 줄에 한 사람만 말하게 한다

## [Q-T10] 따옴표 역할 뒤바뀜 — `quote_role_swapped`

- 근거: `KO-I.14`
- 판정: `[확인]`
- 뜻: **대사 줄 안의 인용**을 큰따옴표로 썼다
- 검사 조건: 화면자막 표식을 큰따옴표(`--fn-marker double_quote`)로 정한 작업에서만 검사한다. 자막 전체가 큰따옴표로 감싸진 화면자막은 정상이라 건너뛴다
- 규정: 화면 글자는 큰따옴표 “ ”, 인용·강조·작품 제목·소리 내어 읽기는 작은따옴표 ‘ ’

## [Q-T11] 화면 글자 자막과 대사 합침 — `forced_narrative_merged_with_dialogue`

- 근거: `KO-I.8`
- 판정: `[확인]`
- 뜻: 화면 글자 번역(FN)과 대사가 한 자막에 들어 있다
- 고치는 법: 둘을 다른 자막으로 나누고, 겹치면 줄거리에 중요한 쪽을 우선한다

## [Q-T12] 숫자 표기 — `clock_24h` / `currency_converted` / `non_metric_unit`

- 근거: `KO-I.12`
- 판정: `[확인]`
- 구현 상태
  - `non_metric_unit`(미터법 아닌 단위): 구현됨. **화면자막에만** 건다 — 대사는 말한 대로 둔다
  - `clock_24h`(24시간제 시각): **미구현**
  - `currency_converted`(통화 환산): **미구현**
- 규정: 24시간제 금지(군대 맥락 예외), 원화 환산 금지, 도량형은 미터법(원래 단위가 줄거리에 중요하면 예외)

## [Q-T13] 약어 마침표 — `acronym_with_periods`

- 근거: `KO-I.1`
- 판정: `[자동]`
- 뜻: `D.N.A.`처럼 약어 글자 사이에 마침표가 있다. `DNA`로 붙인다

## [Q-T14] 반복 번역 — `repeated_phrase_translated_twice`

- 근거: `KO-I.16`
- 상태: **미구현 검사**
- 규정: 같은 화자가 한 자막에서 반복한 말은 한 번만 번역한다

## [Q-T15] 노래 가사 표기 — `lyric_line_end_punctuation` / `lyric_quotes_or_italics`

- 근거: `KO-I.17`
- 상태: **미구현 검사**
- 규정: 가사 줄 끝은 물음표·느낌표만, 따옴표·기울임꼴 없음

## [Q-T16] 줄바꿈 위치 — `line_break_position`

- 근거: `GR-4`, `KO-I.11`
- 판정: `[확인]` (SDH는 `S16`로 같은 검사)
- 뜻: 한국어 문법으로 보아 떼면 안 되는 자리에서 줄을 나눴다
- 검사 조건: 언어가 한국어일 때만 검사한다

## [Q-T17] 존대·반말 어긋남 — `formality_inconsistent`

- 근거: `KO-I.19`
- 판정: `[확인]`
- 뜻: 같은 인물 관계에서 존댓말·반말이 오락가락한다
- 필요한 것: 인물별 말투 표(`--cast 파일.tsv`, `U-10`)
- 규정: 성별·나이만으로 존대를 정하지 않는다

## [Q-T18] 위가 무거운 줄바꿈 — `line_break_top_heavy`

- 근거: `KO-I.11` (권고)
- 판정: `[확인]` (SDH는 `S17`로 같은 검사)
- 뜻: 윗줄이 아랫줄보다 길다. 위반이 아니라 **권고**다 — 아래 줄이 긴 모양을 선호한다

## [Q-T19] 쓰지 않는 문장부호 — `forbidden_punctuation`

- 근거: `KO-I.13` (SDH는 `S36`)
- 참고: SDH 번호는 전에 `S17`이었다. 위가 무거운 줄바꿈(`Q-T18`, SDH `S17`)과 번호가 겹쳐 2026-09-17에 `S36`으로 바뀌었다 — 옛 리포트의 `S17`은 두 검사 중 하나일 수 있다
- 판정: `[확인]`
- 뜻: 프로파일이 금지한 문장부호가 대사에 들어갔다(예: 대본의 `이름:` 화자 표시 콜론이 자막에 그대로 실림)
- 화자 표시·효과음 괄호 안은 이 검사가 보지 않는다

## [Q-S01] 한 줄 글자 수 초과 (SDH) — `line_length_exceeded`

- 근거: `KO-II.2`
- 판정: `[확인]`
- 뜻: 한 줄 16자 초과(라틴 문자·공백·문장부호 0.5자). 번역 자막과 같은 기준이다
- 참고: 화자 표시·효과음 괄호도 글자 수에 들어간다. 짧게 쓴다(`KO-II.10`)

## [Q-S02] 읽기 속도 초과 (SDH) — `reading_speed_exceeded`

- 근거: `KO-II.3`
- 판정: `[확인]`
- 뜻: 초당 글자 수가 성인 14자, 어린이 11자를 넘는다. **번역 자막(12자/9자)보다 높다**
- 고치는 법: SDH는 의역하지 않는다. 줄이기·삭제·압축으로 손본다(`KO-II.1`)

## [Q-S05] 금지된 외국어 표시 — `forbidden_foreign_marker`

- 근거: `KO-II.6`
- 판정: `[확인]`
- 뜻: `[외국어로 말한다]`를 썼다
- 고치는 법: 무슨 언어인지 확인해 `[일어로 말한다]`처럼 쓴다

## [Q-S06] 효과음 어미 — `discouraged_sound_ending`

- 근거: `KO-II.9`
- 판정: `[확인]`
- 뜻: 효과음 설명이 "들린다"나 "소리"로 끝난다
- 고치는 법: `[콜록거리는 소리]` → `[기침]`, `[콜록거린다]`

## [Q-S07] 말더듬 표시 — `stutter_label`

- 근거: `KO-II.9`
- 판정: `[확인]`
- 뜻: `[말을 더듬으며]`를 썼다
- 고치는 법: 실제 말더듬이 있는 인물이 아니면 받아쓰기(`그… 그게`)나 `[주저하며]`

## [Q-S08] 음표 띄어쓰기 — `music_note_spacing`

- 근거: `KO-II.7`
- 판정: `[자동]`
- 뜻: 음표(♪)와 가사 사이에 공백이 없다. 한 칸 띄운다

## [Q-S09] 음표 짝 — `music_note_unpaired`

- 근거: `KO-II.7`
- 판정: `[확인]`
- 뜻: 가사 자막 앞뒤 음표 중 하나가 빠졌다

## [Q-S10] 삐 처리 별표 — `bleep_asterisk_count`

- 근거: `KO-II.10`
- 상태: **미구현 검사**
- 규정: 단어 일부는 음절당 별표 하나, 구·문장 전체는 `****`

## [Q-S11] 닫히지 않은 괄호 — `bracket_unclosed`

- 근거: `KO-II.8`, `KO-II.9`
- 판정: `[확인]`
- 뜻: 화자 표시나 효과음의 대괄호가 닫히지 않았다
- 기계가 고치지 않는 이유: 어디서 닫을지는 사람만 안다

## [Q-S12] 2인 화자 문장 이어짐 — `sentence_split_across_events`

- 근거: `KO-II.5`
- 상태: **미구현 검사**
- 규정: 2인 화자 자막의 각 줄은 그 안에서 끝나는 문장이어야 한다

## [Q-S13] 화자 표시 일관성 — `speaker_id_inconsistent`

- 근거: `KO-II.8`
- 판정: `[확인]`
- 뜻: 같은 인물의 화자 표시가 들쭉날쭉하다
- 주의: 이야기가 진행되며 표시가 바뀌는 것(`[경찰]` → `[김 경위]`)은 **같은 인물임이 화면으로 분명하면** 허용된다. 그래서 사람이 확인한다
