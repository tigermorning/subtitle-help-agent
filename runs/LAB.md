# 실험 기록 — 자막 생성기 소통 창구 에이전트

- 규칙
  - eval 분할은 프롬프트에 넣지 않는다
  - 한 번에 하나만 바꾼다
  - 바꾼 것과 숫자를 적는다. 흔들림이 의심되면 두 번 잰다
- 근거는 평가셋이 아니라 `docs/`(근거 문서·매핑표)에서만 가져온다
- 로그: `runs/<번호>-<이름>.log`, 결과 원본: `runs/<번호>-<이름>.json`

## 채점기 검증

| # | 내용 | 결과 |
|---|---|---|
| 00 | 모범 답안을 채점기에 넣는다 | 1차 49/51 → 고친 뒤 **51/51**. 04에서 57건으로 늘린 뒤 **57/57** |
| 00 | 빈 답변("확인해 보겠습니다")을 넣는다 | 1차 3/51 통과(너무 후함) → 고친 뒤 **0/51**. 57건에서 **0/57** |

- 1차에서 고친 것
  - `U-006`: 채점기가 must 항목 하나를 판정 목록에서 빠뜨렸는데 통과로 셌다 → 항목 번호로 판정받고, 빠지면 한 번 더 묻고, 그래도 빠지면 누락으로 센다
  - `F-001`: 모범 답안이 "(접수번호 안내)" 자리표시라 접수번호가 없었다 → 예시 접수번호로 채움
  - `F-004`·`F-005`·`F-009`: 빈 답변을 "접수했다"로 봐줬다 → 행동 항목은 명시적 문장이 있어야 인정
- **재검증 (2026-09-17, 실험 13 뒤)** — 12·13이 모범 답안에서 규칙 번호·명령줄을 걷어냈으므로 다시 돌렸다
  - 모범 답안 57/57 · 빈 답변 0/57 · 여러 턴 모범 답안 24/24 · 여러 턴 빈 답변 0/24 (`runs/14-judge-validate.json`)
  - 채점기 코드는 그대로다. 바뀐 것은 채점 대상인 모범 답안 문구뿐이고, 판정은 변하지 않았다
- 검증기(`guardrail.py`)도 모범 답안으로 돌렸다: 1차 1/51 위반
  - `O-008` 모범 답안이 근거 조각(R-04)에 없는 `--translate`를 언급 — 검증기가 맞았다. 모범 답안을 고침

## 실험

| # | 바꾼 것 | 분류 정확도 | macro F1 | 도구 호출 | 답변 | 둘 다 | forbid | 메모 |
|---|---|---|---|---|---|---|---|---|
| 01 | 기준선 | 0.878 | 0.879 | 33/41 (80.5%) | 36/41 (87.8%) | 32/41 (78.0%) | 0 | 검증 재생성 0 · 되묻기 0 |
| 02 | R-01 문구: 넘기기 전에 규정 검색을 다시 하지 않는다(사용자 결정) | 0.878 | 0.879 | 36/41 (87.8%) → 2회차 37/41 (90.2%) | 36/41 (87.8%) → 2회차 38/41 (92.7%) | 35/41 (85.4%) → 2회차 37/41 (90.2%) | 0 | O-001~O-004 도구 통과. 두 번 쟀다 — 아래 |
| 03 | 근거 문서 확장(넷플릭스 관련 공개 문서 19건 → `31_netflix_related.md`) + 원문 발췌를 근거로 함께 반환 | 0.878 | 0.879 | 38/41 (92.7%) → 2회차 36/41 (87.8%) | 36/41 (87.8%) → 2회차 36/41 (87.8%) | 36/41 (87.8%) → 2회차 36/41 (87.8%) | 0 | 02와 흔들림 범위 안. 새 문서를 재는 문항이 아직 없다 — 아래 |
| 04 | 평가셋 확장: 새 문서(`NR-*`)용 문항 6건(eval 5 · fewshot 1). 코드·프롬프트 변경 없음 | 0.804 → 2회차 0.848 | 0.794 → 2회차 0.853 | 41/46 (89.1%) → 41/46 (89.1%) | 39/46 (84.8%) → 41/46 (89.1%) | 39/46 (84.8%) → 40/46 (87.0%) | 1 → 0 | 평가셋이 바뀌어 01~03과 직접 비교 불가. 기존 41건만 떼면 둘 다 36·36(03과 같음) — 아래 |
| 05 | 분류 지침 STYLE_RULE 정의에 새 문서 범위(FN·템플릿·부가/마케팅 영상·제목 번역·현지화 모범 사례·SDH/CC·FAQ) 추가 | 0.913 → 2회차 0.891 | 0.904 → 0.887 | 43/46 (93.5%) → 41/46 (89.1%) | 43/46 (93.5%) → 41/46 (89.1%) | 43/46 (93.5%) → 41/46 (89.1%) | 0 → 0 | 새 5건 두 번 다 5/5(04: 3·4). 기존 41건 38·36 — 아래 |

### 01 기준선 — 실패 9건의 원인

| 원인 | 건수 | 문항 | 무엇이 틀렸나 |
|---|---|---|---|
| 넘기기 전에 규정 검색을 한 번 더 함 | 4 | O-001~O-004 | `refer_to_nikl` + `search_style_rule`. 답은 맞음. **R-01 조항("먼저 30 문서에 조항이 있는지 본다")과 평가셋 기대 도구가 어긋난다** — 아래 |
| 접수 대신 정보만 요청 | 2 | F-002, F-008 | 재현 정보를 먼저 물으며 "접수할 수 있습니다"로 끝냄. `submit_feedback` 미호출 |
| 접수 대신 검사 설명 | 1 | F-007 | 대본 붙여넣기 금지는 안내했으나 `get_check_info`만 부르고 접수 안 함 |
| 오분류 | 1 | F-004 | 쿠팡플레이 프로파일 **추가 요청**을 "넷플릭스 외 발주처 규정" 경계 규칙으로 OUT_OF_SCOPE 처리 |
| 사실 누락 | 1 | S-001 | 올바른 조각(KO-II.8)을 읽고도 "한 명뿐이면 1을 붙이지 않는다"를 빠뜨림 |

- 점수에 안 잡힌 것
  - 오분류는 5건인데 실패로 잡힌 것은 F-004 하나다. 나머지 4건은 다른 카테고리로 가고도 도구·답변이 통과했다
    - U-004·U-006 → FEEDBACK: FEEDBACK 규칙이 `search_usage`를 먼저 부르게 해서 통과
    - S-008 → OUT_OF_SCOPE, O-007 → STYLE_RULE: 분류가 틀려도 답변 단계가 맞는 도구를 골랐다
    - 분류 점수와 최종 답변 품질이 갈리는 자리 — 분류만 고치다 답변을 깨지 않게 둘 다 잰다
  - 평가를 돌리면 `submit_feedback`이 실제로 `data/feedback/`에 접수 기록을 남긴다(저장소 제외)

### 02 R-01 — 넘기기 절차를 문서에서 바로잡음

- 바꾼 것: `docs/50_referral_nikl.md` R-01. "먼저 30 문서에 조항이 있는지 본다" → "조항이 있는지는 분류 단계가 이미 가렸다. 여기 온 질문은 규정 검색을 다시 하지 않는다"
  - 이유: 문서와 평가셋 기대 도구가 어긋났다. 사용자가 "검색 없이 바로 넘긴다"로 결정(2026-09-17)
  - 프롬프트(`prompts.py`)는 건드리지 않았다
- 결과: O-001~O-004 네 건 모두 도구 통과(두 번 다)
- **흔들림이 크다** — 같은 설정 두 번에 답변 통과가 36 → 38
  - 1회차에만 실패: Q-008(must 문구 "'소리'나 '들린다'" 중 '소리'만 말함 — 채점이 엄격), F-004
  - 두 번 다 실패: S-008(OUT_OF_SCOPE로 오분류 후 도구 없이 범위 밖 안내), F-002·F-007·F-008(접수 안 함)
  - 기준선에서는 S-008이 오분류돼도 통과했다 — 오분류 뒤 답변 단계의 선택도 흔들린다
- 다음 판정부터는 **두 번 재서 둘 다 본다**. 1건 차이는 흔들림으로 본다

### 03 근거 문서 확장 + 원문 발췌

- 바꾼 것 (사용자 결정 2026-09-17: 근거 문서 크롤링 허용, 원문은 로컬 근거로만)
  - 넷플릭스 파트너헬프 공개 글 19건을 더 받아 `docs/31_netflix_related.md`(조각 19개)로 요약
  - `originals.py`: 요약 조각마다 원문의 해당 절을 찾아 도구 결과에 `original`로 함께 싣는다
  - 검증기(`guardrail.py`)가 원문 발췌도 근거로 인정한다. 조각 ID 형식이 `NR-TPL-3`처럼 길어져 인용 검사 정규식을 넓혔다
  - `prompts.py` 답변 규칙 4번: 원문이 1차 근거, 요약과 다르면 원문을 따르고 밝힌다
- 두 가지를 한꺼번에 바꿨다 — 한 번에 하나 원칙에서 벗어났다. 둘 다 "근거를 늘린다"는 같은 변경의 앞뒤라 묶었다
- 결과: 02와 흔들림 범위 안(둘 다 통과 35·37 → 36·36). forbid 0, 검증 재생성 0
  - 모범 답안 51건 검증기 오탐 0 (원문 발췌 포함 상태로 다시 확인)
- **이 실험은 새 문서의 효과를 재지 못한다** — 평가셋에 `NR-*` 조각을 근거로 하는 문항이 없다. 기존 문항이 깨지지 않았다는 것만 확인했다
- 이번 실패 원인은 02와 같다
  - 두 번 다: S-008(오분류), F-007·F-008(접수 안 함)
  - 한 번씩: U-006(FEEDBACK으로 오분류 뒤 엉뚱한 조각), F-002·F-004, F-003(채점: "오탐 제보로 접수"를 "제보를 접수"로만 말함)

### 04 평가셋 확장 — 새 문서용 문항 6건

- 추가 문항 (모두 STYLE_RULE → 카테고리 문항 수가 STYLE_RULE 17건으로 치우쳤다)
  - S-012 줄당 글자 수(`NR-CPL`), S-013 예고편 읽기 속도(`NR-MKT-2`), S-014 예고편 번역자 크레디트(`NR-MKT-3`)
  - S-015 FN과 전체 자막(`NR-FN`), S-016 피벗 템플릿(`NR-TPL-1`), S-017 SDH와 CC(`NR-SDH`, fewshot)
- 채점기·검증기 검증: 모범 답안 57/57 통과, 빈 답변 0/57, 검증기 오탐 0/57
- **사용자 정정 반영 (2026-09-17)**: "자동 QC 반려선 23자는 의미 없는 값이다. 보통 16~17자다"
  - `NR-CPL`에 실무 기준을 사용자 확인 사항으로 적음(공식 문서 근거 아님). 창구는 23자를 작업 기준으로 안내하지 않는다
  - S-012를 "23자까지 써도 되나"를 묻고 16자 기준으로 답하는 문항으로 바꿈
- 결과 (기존 41건 / 새 5건으로 나눠 봄)

| | 기존 41 둘 다 통과 | 새 5 둘 다 통과 |
|---|---|---|
| 1회차 | 36 | 3 |
| 2회차 | 36 | 4 |

- 기존 41건은 03과 같다 — 문항 추가가 기존 결과를 흔들지 않았다
- 새 문항 실패
  - **S-016 피벗 템플릿 — 두 번 다 오분류**(USAGE, OUT_OF_SCOPE). 분류 지침(`prompts.py` ROUTE_GUIDE)의 STYLE_RULE 정의에 템플릿·FAQ·마케팅 영상 같은 새 문서 범위가 없다. 03에서 검색 범위만 넓히고 분류 지침은 안 고쳤다
  - S-012 1회차: 16자 기준은 말했지만 "20자는 자동 반려 한도 내"를 먼저 말해 forbid 위반. 2회차 통과

### 05 분류 지침을 매핑표에 맞춤

- 바꾼 것: `prompts.py` ROUTE_GUIDE의 STYLE_RULE 정의에 `docs/31_netflix_related.md` 범위를 넣음. 매핑표(`docs/MAPPING.md` 2절)에는 03에서 이미 들어가 있었다
- 결과 (04와 같은 46건)

| | 기존 41 둘 다 통과 | 새 5 둘 다 통과 | 분류 정확도 |
|---|---|---|---|
| 04 | 36 · 36 | 3 · 4 | 0.804 · 0.848 |
| 05 | 38 · 36 | **5 · 5** | 0.913 · 0.891 |

- S-016(피벗 템플릿) 오분류 해소, S-012(23자) 두 번 다 통과
- 기존 41건은 흔들림 범위 안 — 새 정의가 기존 문항을 깨지 않았다
- 남은 실패는 02부터 같은 자리
  - 두 번 다: F-004(쿠팡 프로파일 추가 요청 → OUT_OF_SCOPE), F-007·F-008(접수 안 함)
  - 한 번: S-008(존대 질문 → OUT_OF_SCOPE), F-002(접수 안 함)
  - 오분류지만 통과: U-004·U-006(→ FEEDBACK), O-007(→ STYLE_RULE)

## 새 평가 데이터 기준선 (06) — 분류 120 · 어려운 문항 72 · 여러 턴 12

- 데이터 설명·검증: `data/README.md`. 코드·프롬프트는 05와 같고, 대화 맥락(`history`)을 받는 기능만 붙였다
- 모두 두 번 쟀다

| 평가 | 1회차 | 2회차 |
|---|---|---|
| 분류 120 — 정확도 / macro F1 | 0.925 / 0.923 | 0.917 / 0.915 |
| 어려운 문항 72 — 통과 | 62 | 60 |
| 여러 턴 12 — 도구 집합 일치 / 턴 답변 / 대화 전체 | 10 / 24 of 24 / 10 | 10 / 23 of 24 / 10 |

- 여러 턴 수치는 M-02 대체 도구 집합(`tools_union_alt`)을 넣은 기준으로 저장된 결과를 다시 채점한 값이다
  - 처음 채점: 도구 9 · 대화 9 (두 번 다). M-02는 정답셋 저작 때 "두 도구 모두 가능"이라 적어 놓고 한쪽만 인정한 채점 설계 결함이었다

### 분류 120 — 오분류 (두 번 합쳐 19건, 문항 기준 13개)

| 패턴 | 문항 | 메모 |
|---|---|---|
| 특정 **미구현** 검사 번호를 들고 왔는데 USAGE로 | 400032(C03), 400043(T14), 400054(S12) | 매핑표는 "미구현 검사가 뭐냐"만 USAGE. 번호를 들고 온 경우 규칙이 없다 — 라벨 검증 때도 모호로 표시된 유형 |
| 불만 섞인 사용법·검사 질문이 FEEDBACK으로 | 400002, 400018, 400030, 400041, 400059 | "안 잡혀요", "왜 안 해 줘요" 표현에 끌림 |
| 다른 발주처·외부 프로그램 이름이 나오면 OUT_OF_SCOPE로 | 400027(SubtitleEdit 북마크 모으기 = `--bookmarks`), 400028(쿠팡 파일 자동 교정 = `-p coupang --fix`) | 생성기 기능인데 이름에 끌림 |
| 그 밖 | 400044(T15 미구현 → STYLE_RULE, conf 0.78) | |

- STYLE_RULE·FEEDBACK·OUT_OF_SCOPE는 두 번 다 24/24

### 어려운 문항 — 유형별 (1회차 / 2회차)

| 유형 | 통과 | 실패 |
|---|---|---|
| 경계모호 13 | 9 / 9 | 500006·500007 오분류, 500008·500011 되묻기 대신 처리 |
| 극단단답 12 | 10 / 11 | 500016 "SDH는요?" 처리(0.62), 500018 "--fix?" 되물음(0.40) |
| 다중의도 12 | 11 / 8 | 500036 등 확신도 0.5 미만으로 되물음 — 두 요청이 섞이면 확신도가 떨어진다 |
| 분류체계밖 11 | 11 / 11 | |
| 텍스트손상 12 | 12 / 12 | |
| 문맥의존 12 | 9 / 9 | 500064·500066 앞 턴 없는데 처리, 500069 USAGE로 |

### 여러 턴 — 실패

- M-03(버그 제보), M-08(대본 문의 → 제보): 접수 전에 `search_usage`·`get_check_info`를 더 부름 — 1턴 평가의 F-002·F-007·F-008과 같은 원인
- 2회차 M-08 2턴: "원인을 단정한다" forbid 위반 1건

## 07 경계 규칙 보강 — 미구현 항목 번호 · 불만 표현 · 다른 이름

- 바꾼 것: `docs/MAPPING.md` 3절에 규칙 3행 추가 → `prompts.py` ROUTE_GUIDE 경계 규칙에 그대로 옮김
  1. 특정 검사 번호를 들고 왔으면 미구현이라고 나왔어도 QC_EXPLAIN
  2. 짜증·불만 **표현만으로** FEEDBACK에 보내지 않는다. FEEDBACK은 요청·오류·문서와 다른 동작·"잘못 잡힌다"고 알리기만 하는 제보. 이유·할 일을 **묻는** 문의는 원래 카테고리
  3. 다른 발주처·프로그램 이름이 나와도 이 생성기 기능을 묻는 것이면 USAGE
- 평가 문항과 겹치지 않게 **구체 예문 없이** 규칙 서술과 틀 형태 예시만 넣었다
- 결과 (06·05 대비, 두 번씩)

| 평가 | 전 | 07 |
|---|---|---|
| 분류 120 — 정확도 | 0.925 · 0.917 (06) | **0.967 · 0.967** |
| 1턴 57 중 eval 46 — 둘 다 통과 | 43 · 41 (05) | 41 · 42 |
| 1턴 eval 46 — 분류 정답 | 42 · 41 (05) | 42 · 41 |
| 어려운 문항 72 — 통과 | 62 · 60 (06) | 60 · 58 |
| 여러 턴 12 — 대화 통과 | 10 · 10 (06) | 9 · 8 |

- **분류 120은 확실히 올랐다** — 오분류 9·10건 → 4·4건
  - 미구현 항목 번호 3건(400032·400043·400054)과 다른 이름 2건(400027·400028) 두 번 다 해소
  - 남은 것: 불만 섞인 질문이 여전히 FEEDBACK으로 — 400002, 400030, 400041(두 번 다), 400018·400024(한 번씩)
- 1턴 평가: F-004(쿠팡 프로파일 추가 요청 → OUT_OF_SCOPE)가 두 번 다 해소. U-004·U-006 → FEEDBACK은 그대로. 둘 다 통과 수는 흔들림 범위 안
- 어려운 문항·여러 턴이 1~2건씩 내려갔지만 **실패 내용을 보면 이번 규칙과 무관하다**
  - 어려운 문항: 다중의도가 확신도 0.5 미만으로 되묻기로 빠지는 기존 문제(500025·500028·500032·500033·500036), 문맥의존 흔들림
  - 여러 턴: M-07 1턴 "16자" 누락, M-06 1턴 앞 턴 없이 옵션을 추측(2회차만), M-10 2턴 검색이 KO-II.3을 못 찾음(2회차만) — 셋 다 답변·검색 단계 흔들림
- 판정: **유지.** 분류 개선이 두 번 재현됐고 다른 평가의 하락은 이번 변경 밖의 원인이다
- 다음 후보
  - 다중의도 확신도: 두 요청이 섞여도 각 요청의 카테고리가 분명하면 확신도를 낮추지 않게 — 게이트와 답변 단계(두 카테고리 도구)를 함께 봐야 한다
  - 불만 섞인 USAGE 질문: 규칙이 이미 있는데도 남는다 — 예시(routing fewshot 30건) 투입으로 볼 차례
  - 접수 누락(F-007·F-008, M-03·M-08): FEEDBACK 답변 규칙 "접수 먼저"

## 08 분류 예시 30건 추가 (routing_answers fewshot)

- 바꾼 것: `prompts.py` 분류 예시를 goldenset fewshot 11건 → **+ routing fewshot 30건(카테고리당 6건) = 41건**, 카테고리 순으로 묶음
- 누수 확인: 예시 41건이 분류 eval 120·goldenset eval 46·어려운 문항 72·여러 턴 사용자 발화 어디에도 없음(문장 일치 기준)
- 결과 (07 대비, 두 번씩)

| 평가 | 07 | 08 |
|---|---|---|
| 분류 120 — 정확도 | 0.967 · 0.967 | **0.992 · 0.975** |
| 1턴 eval 46 — 분류 정답 | 42 · 41 | **43 · 43** |
| 1턴 eval 46 — 둘 다 통과 | 41 · 42 | 42 · 42 |
| 어려운 문항 72 — 통과 | 60 · 58 | 61 · 58 |
| 여러 턴 12 — 대화 통과 | 9 · 8 | **11 · 10** |
| forbid 위반 | 0 · 0 | 0 · 0 |

- 분류 120: 오분류 4·4 → 1·3
  - 07에서 남았던 불만 섞인 USAGE 질문(400002·400018·400024·400030) 두 번 다 해소
  - 남은 것: 400041(대사 속 "5마일"이 안 잡혔다 → FEEDBACK, 두 번 다), 400059·400084(한 번씩)
- 1턴: 오분류는 두 번 다 U-004·U-006(→ FEEDBACK)·O-007(→ STYLE_RULE) 3건. 07에서 한 번씩 나왔던 S-008·U-007은 해소. U-004·O-007은 오분류돼도 통과, U-006은 2회차에만 실패. 남은 실패는 접수 누락(F-002·F-007·F-008)과 Q-005(1회차) — 분류가 아니라 답변 단계
- 여러 턴: 남은 실패는 M-08(두 번 다)·M-03(2회차) — 접수 전에 다른 도구를 더 부르는 문제
- 어려운 문항: 변화 없음. 다중의도가 확신도 미달로 되묻기로 빠지는 문제(7·6/12)가 그대로 — 예시로는 안 풀린다
- **주의 — 점수 해석**: 예시 30건과 분류 eval 120건은 같은 저작 과정(같은 에이전트·같은 지시)에서 나왔다. 문장은 겹치지 않지만 말투·주제 분포가 비슷해, 분류 120 점수에는 "같은 출처 예시"의 이득이 섞였을 수 있다
  - 저작 출처가 다른 goldenset(46건)에서도 분류 정답이 42·41 → 43·43으로 올라, 이득이 전부 출처 효과는 아니다
- 판정: **유지**
- 다음 후보: 접수 먼저(FEEDBACK 답변 규칙), 다중의도 처리

## 09 접수 먼저 — FEEDBACK 답변 규칙

- 바꾼 것: `prompts.py` ROUTE_RULES["FEEDBACK"] + `docs/40_feedback.md` F-03에 "접수가 먼저" 한 줄
  - 제보가 있으면 이번 턴에 바로 `submit_feedback`, 재현 정보는 접수번호를 알린 같은 답변에서 한 번 묻는다
  - `search_usage`는 ① 기능 유무를 물었을 때 ② 문서 설명과 비교해야 답이 될 때만 먼저
  - 제보에 검사 번호가 나와도 `get_check_info`·`search_style_rule`은 부르지 않는다
  - 대본을 붙이겠다는 제보도 붙여넣지 말라고 안내하되 접수는 한다
- 분류기는 이 규칙을 보지 않으므로 1턴·여러 턴 평가만 쟀다(분류 120·어려운 문항은 08과 같다)
- 결과 (08 대비, 두 번씩)

| 평가 | 08 | 09 |
|---|---|---|
| 1턴 eval 46 — 도구 호출 | 43 · 43 | 43 · 42 |
| 1턴 eval 46 — 둘 다 통과 | 42 · 42 | 42 · 42 |
| 여러 턴 12 — 도구 집합 일치 | 11 · 10 | **12 · 12** |
| 여러 턴 12 — 대화 통과 | 11 · 10 | **11 · 11** |

- **고쳐진 것 (두 번 다)**: F-002(바로 접수), F-008(문서 확인 뒤 접수), 여러 턴 M-03·M-08
- **새로 깨진 것 (두 번 다)**: U-004·U-006
  - 둘 다 원래 FEEDBACK으로 **오분류**되던 문항이다. 08까지는 FEEDBACK 규칙이 `search_usage`를 먼저 부르게 해서 오분류가 가려졌다. "접수 먼저"가 그 안전망을 걷어 내 오분류가 그대로 실패로 드러났다
- **여전히 실패**: F-007(대본 붙여넣기 + 오탐) — 붙여넣지 말라는 안내만 하고 접수를 안 한다. 1회차는 QC_EXPLAIN으로 오분류
- 새로 두 번 다 실패: M-10 2턴 "그럼 넷플릭스 SDH는요?" — 검색이 KO-II.3을 못 찾음. 이번 변경과 무관(07 2회차에도 한 번 나옴), 앞 턴 맥락이 검색어에 안 들어가는 문제
- **판정 보류 — 라벨과 규칙이 어긋난다**
  - U-006 "프로그램이 죽었어요 … 처음부터 다시 돌려야 하나요?": 매핑표 규칙("오류가 나면 사용법 확인이 섞여 있어도 FEEDBACK")대로면 FEEDBACK이 맞다. 그런데 goldenset 정답은 USAGE + `search_usage`
  - U-004 "효과음 지적이 하나도 안 나와요 (-p·-l만 줬어요)": 07 규칙 ④ "잡혀야 할 게 안 잡힌다고 알리는 제보"로 읽힐 수 있다. 정답은 USAGE(-k 누락 안내)
  - 사용자 결정 필요 — 규칙을 고칠지, 두 문항을 "접수 + 사용법 안내"(도구 둘)로 바꿀지

## 10 증상 + 해결 방법 질문은 USAGE (사용자 결정)

- 사용자 결정(2026-09-17): 문서로 원인·해결이 설명되는 증상을 오류·미탐처럼 말한 문의는 **사용법 안내가 정답**. 평가셋은 그대로 두고 규칙을 좁힌다
- 바꾼 것: `docs/MAPPING.md` 3절 1행 추가 → `prompts.py` ROUTE_GUIDE의 "오류·문서와 다른 동작은 FEEDBACK(사용법 확인이 섞여 있어도)"를 교체
  - FEEDBACK: 고장·오탐을 **알리거나 고쳐 달라는** 제보, "원래 이래요?"처럼 **동작이 정상인지** 묻는 제보
  - USAGE: 증상을 말해도 **해결 방법**("다시 해야 하나요", "어떻게 해야 해요")을 묻는 문의
  - 분류기는 문서를 못 보므로 "해결 방법을 묻는가"를 신호로 삼았다. 평가 문항에 맞춘 표현은 넣지 않았다
- 결과 (09·08 대비, 두 번씩)

| 평가 | 전 | 10 |
|---|---|---|
| 1턴 eval 46 — 둘 다 통과 | 42 · 42 (09) | **44 · 45** |
| 1턴 eval 46 — 분류 정답 | 43 · 43 (08) | 42 · 44 |
| 분류 120 — 정확도 | 0.992 · 0.975 (08) | 0.967 · 0.975 |
| 어려운 문항 72 — 통과 | 61 · 58 (08) | 60 · 61 |
| 여러 턴 12 — 대화 통과 | 11 · 11 (09) | 10 · 11 |
| forbid 위반 | 0 · 0 | 0 · 0 |

- 1턴: **U-006 해소(두 번 다)**, F-007도 두 번 다 통과. 남은 실패는 U-004(두 번 다, 예상대로 — 해결 방법을 묻는 표현이 없다)와 U-007(1회차)
- 분류 120: **400105가 새로 두 번 다 틀림** — "exe를 한글 폴더에 두면 꺼져요. 한글·공백 있으면 안 되는 건가요?"(정답 FEEDBACK)를 USAGE로. 원인을 묻는 표현이 새 규칙에 걸렸다. 문서에 없는 증상이라 사용자 원칙상 FEEDBACK이 맞다 — **분류기가 문서를 못 보는 한 이 규칙으로는 둘을 못 가른다**
  - 400002·400024(불만 섞인 USAGE → FEEDBACK)가 1회차에 다시 나옴. 400018은 두 번 다
- 여러 턴: M-10(검색 누락)은 이번 변경과 무관. M-07은 1회차만
- 판정: **유지** — 1턴 +2·+3, 분류 120은 -1·0으로 흔들림 수준. 다만 400105가 보여 주듯 "문서로 설명되는가"는 분류 단계에서 끝까지 가를 수 없다
- 구조적 대안(다음 후보): FEEDBACK으로 분류된 문의도 답변 단계에서 `search_usage`로 증상을 문서와 대조하고, 문서가 설명하면 사용법 안내 + 접수 안 함으로 마무리. 09의 "접수 먼저"와 충돌하므로 설계 결정이 필요하다

## 11 질문자가 고른 카테고리 검증 (데모 요구사항, 사용자 결정 2026-09-17)

- 요구: 직접 질문은 카테고리를 먼저 고르게 하되, 고른 카테고리를 그대로 믿지 말고 질문마다 맞는지 검증한다
- 구현: `agent.check_category` — 고른 값은 분류기에 **넘기지 않는다**(넘기면 분류기가 따라가 검증이 안 된다). 분류 결과와 대조만 한다
  - match: 그대로 / mismatch: 알리고 분류기 판단대로 답한다(사용자 결정) / unclear: 분류기 확신도 미달이면 되묻는다
- 측정 `python evaluate.py --category-check` — 분류 eval 120건마다 "맞게 고름"과 "틀리게 고름(다른 카테고리 하나, 씨앗 고정)"을 흉내 낸다. 분류는 선택과 무관해 질문당 분류기 1회 호출

| | 1회차 | 2회차 |
|---|---|---|
| 맞게 고른 경우 match | 116/120 | 115/120 |
| 맞게 골랐는데 뒤집음(mismatch) | 4 | 5 |
| 틀리게 고른 경우 바로잡음 | 116/120 | 115/120 |
| 틀린 선택을 그대로 받아들임 | 1 | 1 |
| 다른(틀린) 카테고리로 바꿈 | 3 | 4 |

- 실패는 분류 오분류와 같은 문항이다(400002·400018·400041·400105) — 카테고리 검증의 정확도는 분류 정확도가 그대로 정한다
- "그대로 받아들임" 1건은 400002: 질문자가 FEEDBACK으로 틀리게 골랐고 분류기도 FEEDBACK으로 틀렸다 — 선택과 오분류가 같은 쪽으로 겹치면 못 잡는다
- **API 사용 제한(2026-09-17)** 이후로는 이 측정을 다시 돌리지 않았다. 화면 흐름은 `tests/test_offline.py`(가짜 모델 응답, 모델 호출 차단 확인 포함)로만 확인한다

## 12 규칙 번호를 사용자에게 보이지 않기 (사용자 결정 2026-09-17, 측정 안 함)

- 지적: 생성기 리포트의 규칙 번호(`S02`·`T05`)는 생성기 사용자가 모른다 — 질문에 넣으면 안 된다
- 범위(사용자 선택): 데모 + 평가 데이터 전부, 답변에서도 번호를 뺀다
- 바꾼 것
  - 검사 설명 조각 ID `Q-C01`·`Q-S02` 등 → `Q-01`~`Q-33`. 제목은 내용만, 번호·검사 이름은 조각마다 "리포트 표시" 한 줄
  - `get_check_info(item)` — 검사 항목 내용으로 찾는다(2-gram 검색). 리포트 표시를 붙여 넣어도 찾는다
  - 분류·답변 지침, 되묻기 문구, 데모 설명에서 번호 예시를 내용 예시로
  - 검증기에 "내부 표기 노출"(규칙 번호 `[CTSK]\d{2}`, 밑줄 2개 이상 검사 이름) 추가
  - 데이터: 준비된 Q&A 18건, goldenset 11문항, 멀티턴 3대화, inquiries 29행(+`intent_full` 라벨 30행), hard_cases 12행
  - 한국어 교정 결과 표시 `K01`/`K02`도 같은 이유로 "교정 제안"/"확인 필요"로
  - hard 500015("SDH 읽기 속도 초과?")는 '경고'라는 말이 빠져 `STYLE_RULE`을 대안 카테고리로 추가
- 확인(API 없음): `python faq.py` 79/79, `tests/test_offline.py` 40개 통과(규칙 번호 누출 검사 포함)
- **다시 재지 않았다** — 질문 문구가 바뀌어 실험 10 수치와 그대로 견줄 수 없다. 재측정은 API 승인 뒤

## 13 명령어를 안내하지 않는다 (사용자 결정 2026-09-17, 측정 안 함)

- 지적: 이 창구를 쓰는 사람은 **코딩을 모르는 작업자**다. 명령어·명령줄 옵션은 알 필요도 없고 알아서도 안 된다
- 범위(사용자 선택): 근거 문서·준비된 Q&A·평가 데이터·답변·검증기 전부
- 바꾼 것
  - `docs/10_usage.md`를 **데스크톱 앱(`app/`)과 Subtitle Edit 플러그인(`plugin/se4/`) 화면 문구 기준으로 새로 썼다** — `U-01`~`U-17`(전 16개)
    - 메뉴·단추·체크 이름만 쓴다. 명령줄에만 있는 기능(장르, 북마크 모으기, 정답 대조, 리포트 파일, 끌어다 놓기용 배치 파일, 역번역, OCR 1단계 목록 보기 등)은 뺐다
    - 설치·실행 문제는 `[도움말 → 진단...]`의 `[없음]` 줄을 제보하도록 안내한다(`U-14`)
  - `guardrail.py`에 **"명령어 노출"** 추가 — 답변 속 `python`·`pip`·`winget`·`ollama`·`.bat`·`-x` 꼴을 잡는다
  - `prompts.py`(답변 절대 규칙 4, 분류 정의, FEEDBACK 재현 정보 요청), `router.py` 되묻기 문구, `tools.py` `search_usage` 설명, `app.py` 카테고리 설명·예시 문구
  - `docs/MAPPING.md` 6절에 결정을 적었다
  - 데이터: 준비된 Q&A(`faq.json`) 전면 개편, goldenset 12문항, 여러 턴 3대화(M-03·M-04·M-06), `inquiries.csv` 22행, `hard_cases.csv` 10행
    - 근거가 사라진 문항은 **화면에서 할 수 있는 다른 질문으로 바꿨다**(예: 역번역 개수 → 감수 `회차`, `--genre` → 작업 기준 창에서 새 발주처 기준 만들기)
    - `F-008`(끊긴 뒤 이어서 읽기)은 **"근거에서 확인되지 않는다 + 제보 접수"**로 바꿨다 — 없는 기능을 있다고 답하지 않는다
- 확인(API 없음)
  - `python faq.py` **79/79**
  - `tests/test_offline.py` **48개 전부 통과** — 근거 문서·Q&A·평가 데이터 4종에 명령줄 내용이 없는지 보는 시험 8개를 새로 넣었다
  - 조각 ID 대조: 데이터가 인용하는 근거 ID가 전부 문서에 있다(문서에 없는 ID 0건)
- **다시 재지 않았다** — 질문 문구가 또 바뀌어 실험 10·12 수치와 그대로 견줄 수 없다. 재측정은 API 승인 뒤(실험 12와 같은 사유)

### 재측정 (사용자 승인 2026-09-17)

- 실험 11·12는 API 사용 제한으로 못 쟀다. 이번에 승인받아 **실험 10 이후 누적분(11·12·13)을 한꺼번에** 다시 쟀다
  — 그래서 아래 차이는 13만의 효과가 아니다
- 재는 도중에 **평가 데이터에 명령줄이 남아 있는 것을 두 번 더 찾아 고쳤다**. 라운드를 나눠 적는다

| 평가 | 실험 08·10 | 13-a (데이터 1차) | 13-b (누출 더 걷어낸 뒤) |
|---|---|---|---|
| 분류 120 — 정확도 | 0.967 · 0.975 (10) | 0.950 → 0.958 | **0.958** |
| 1턴 46 — 둘 다 통과 | 44 · 45 (10) | 41 → 40 | **43** |
| 1턴 46 — 도구 호출 | — | 43/46 | **45/46** |
| 어려운 문항 72 — 통과 | 61 · 58 (08) | 58/72 | **59/72** |
| 여러 턴 12 — 대화 통과 | 10 · 11 (10) | **11/12** (도구 집합 12/12) | 안 다시 쟀다 |
| forbid 위반 | 0 · 0 | 0 | 1 (O-007) |

- **13-a에서 점수가 떨어진 원인은 바꾼 지침이 아니라 내가 고쳐 쓴 평가 데이터였다**
  - `-p netflix -l ko`, `-k sdh`, `%APPDATA%\자막생성기\` 가 goldenset·hard_cases·inquiries에 남아 있었다
    — 시험의 검사 정규식이 `--두 글자 옵션`만 보고 **한 글자 옵션을 안 봤다**
  - `U-004`는 명령줄 옵션을 빼면서 "해결 방법을 묻는" 신호까지 같이 지워져 FEEDBACK으로 분류됐다(실험 10 규칙대로 동작한 것)
  - `hard_cases` 500025·`inquiries` 400015에는 한국어로 "명령/명령어"가 남아 있었다
- **조용한 실패 하나** — `tests/test_offline.py`의 검사 정규식에 낱말 경계 이스케이프가 **제어문자(0x08)로 박혀** `python`·`pip` 검사가 죽어 있었다.
  그래서 "명령줄 내용 없음" 시험이 통과인데도 데이터에 `python -m checker`가 남을 수 있었다. 정규식을 고치고 한 글자 옵션·`%APPDATA%`·한국어 "명령어·터미널"까지 보게 넓혔다
- 남은 실패(13-b)
  - `U-004`·`Q-002`: 답은 맞는데 must 한 줄씩 누락(다시 검사하라는 안내, 저장은 새 이름)
  - `O-007`: 음역 지침 요약 요청에 공개 가이드 범위를 요약해 답했다 — 실험 10에도 있던 항목이고 이번엔 forbid까지 걸렸다
  - 어려운 문항의 병목은 **다중의도 6/12** — 두 요청이 섞이면 확신도 0.42~0.47로 내려가 되묻기로 빠진다(REPORT 회고의 한계 그대로)
- 로그: `runs/13-answer46*.log` · `13-router120-{3rd,4th}.log` · `13-hard72*.log` · `13-multiturn.log`

### 갈라 재기 — 11·12를 각각 그 시점 코드로 되돌려 다시 잰다 (2026-09-17)

- 위 재측정은 11·12·13을 한꺼번에 잰 값이라 **어느 변경의 몫인지 못 갈랐다.** 그래서 그 시점 커밋으로 worktree를 만들어 상태별로 두 번씩 쟀다
  - 실험 11 상태 `0e84b35`(데모 카테고리 검증까지) · 실험 12 상태 `b835085`(규칙 번호 제거까지) · 실험 13 상태 `8e3254b`
  - 상태마다 그 시점 **평가셋으로** 잰다 — 12·13이 질문 문구를 바꿨으므로 같은 잣대가 아니다. 그래서 아래 표는 "같은 시험을 다시 친 점수"가 아니라 **"그 시점의 시험과 그 시점의 코드"**다

| 상태 | 1턴 46 둘 다 통과 | 1턴 46 도구 호출 | 분류 120 정확도 | forbid 위반 |
|---|---|---|---|---|
| 10 (증상+해결은 사용법) | 44 · 45 | 44 · 45 | 0.967 · 0.975 | 0 · 0 |
| 11 (데모 카테고리 검증) | 41 · 43 | 43 · 44 | 0.967 · 0.967 | 0 · 1 |
| 12 (규칙 번호 감추기) | 42 · 45 | 44 · 45 | 0.950 · 0.958 | 1 · 0 |
| 13 (명령어 안내 금지) | 43 | 45 | 0.958 · 0.958 | 1 |

- **읽는 법**
  - **11은 답변 경로를 건드리지 않았다**(고른 카테고리를 분류기에 넘기지 않는 데모 기능이다). 그런데도 1턴이 41·43으로 흔들렸다 — 같은 상태 안에서 **두 번의 차이가 2건**이라는 뜻이고, 10과의 차이(44·45)도 그 폭 안이다
  - **12에서 분류가 한 칸 내려앉았다**(0.967·0.975 → 0.950·0.958). 규칙 번호를 뺀 질문 문구가 분류 신호를 조금 약하게 만든 것으로 보인다. 13(0.958·0.958)이 그 자리를 그대로 유지한다 — **13이 떨어뜨린 것이 아니다**
  - 1턴은 11·12·13 어디서도 10의 범위(44·45)를 뚜렷이 벗어나지 않는다. 12의 2회차는 45로 가장 높았다
- **그래서 결론**: 규칙 번호 감추기(12)·명령어 안내 금지(13)는 **점수를 사는 변경이 아니라 요구 사항을 맞춘 변경**이고, 치른 값은 분류 정확도 약 0.01~0.02다. 되돌릴 이유는 없다(둘 다 사용자 결정)
- 흔들림이 1턴 기준 ±2건이므로, **1~2건 차이로 판정을 바꾸지 않는다**(01~10에서 쓰던 기준 그대로)
- 로그: `runs/iso-11-*.log` · `runs/iso-12-*.log` (결과 원본은 같은 이름 `.json`)

## 15 증상 제보도 사용법 문서와 대조 — 접수 먼저는 유지 (사용자 결정 2026-09-18)

- 배경: 실험 10 결론 — FEEDBACK·USAGE 경계는 문장 겉모양으로 끝까지 못 가른다. 분류기는 문서를 못 본다
- 사용자 결정: "접수 먼저" 유지 + 접수 경로에서 문서 대조 추가(문서에 있으면 접수 생략 안은 기각)
- 바꾼 것
  - `docs/40_feedback.md` F-05에 "증상 제보도 사용법 문서와 대조한다" 추가
  - `prompts.py` ROUTE_RULES["FEEDBACK"]: 증상 제보는 `submit_feedback` + `search_usage` 같은 턴에 둘 다. 조각이 그 증상을 직접 설명할 때만 전하고, 정상·버그 여부는 단정하지 않는다
  - 채점: 1턴 goldenset에 `expected_tools_alt` 도입(여러 턴 `tools_union_alt`와 같은 방식). F-002·F-003·F-007·F-010, 여러 턴 M-03·M-07·M-08에 `search_usage` 포함 집합을 대체로 인정
- 분류기는 이 규칙을 보지 않으므로 1턴 46·여러 턴 12만 두 번씩 쟀다

| 평가 | 13-b | 15 |
|---|---|---|
| 1턴 46 — 둘 다 통과 | 43 | 39 · 39 |
| 1턴 46 — 도구 호출 | 45 | 43 · 45 |
| 1턴 FEEDBACK 8건 — 둘 다 통과 | — | 8 · 7 |
| 여러 턴 12 — 대화 통과 | 11 (13-a) | 9 · 11 |
| forbid 위반 | 1 | 0 · 0 |

- **FEEDBACK 경로**: 증상 제보(F-002·F-003·F-007·F-008) 전부 두 도구를 불렀고 접수는 한 건도 빠지지 않았다. 순수 기능 요청(F-001·F-004·F-005)은 접수만
- **1턴 하락 4건은 이번 변경 밖이다** — 실패 문항이 전부 FEEDBACK이 아닌 카테고리(U-004·U-005·U-008·Q-002·Q-004·S-005·S-008·S-013·S-016·O-007). FEEDBACK 규칙은 FEEDBACK으로 분류된 문의에만 붙는다(`context.py`)
  - 13-b 뒤 커밋 `65a3c41`(한 줄 글자 수 안내)도 들어가 있어 13-b와 같은 잣대가 아니다. 현재 HEAD 기준선은 따로 재지 않았다
- 새로 보인 약점
  - F-008 2회차: 문서 조각(`.work` 폴더)을 전하느라 "이어서 읽는 설명은 근거에서 확인되지 않는다"를 빠뜨림
  - F-002: 증상과 느슨하게만 이어지는 조각(자막 QA 반영·원본 보존)을 덧붙임 — "직접 설명할 때만" 지시가 완전히 지켜지진 않는다
- **이번 측정이 못 본 것**: 겨냥한 U-004·U-006은 두 번 다 USAGE로 바르게 분류돼 FEEDBACK 경로의 안전망이 시험되지 않았다. 400105(FEEDBACK → USAGE 오분류)는 분류 단계 문제라 이 변경과 무관
- 판정: **유지** — 접수 누락 0, forbid 0, FEEDBACK 문항 유지. 약점 두 개는 다음 후보
- 로그: `runs/15-answer46*.log` · `runs/15-multiturn*.log`

## 16 FEEDBACK 조각 전달 조건 좁히기 + HEAD 기준선

- 바꾼 것 (15의 약점 두 개)
  - `prompts.py` FEEDBACK 규칙: "직접 설명"은 조각이 사용자가 말한 **그 동작 자체**를 다룰 때만. 같은 기능·화면·폴더 이야기뿐이면 쓰지 않는다
  - 동작이 정상인지 물었는데 설명 조각이 없으면 "문서에서 확인되지 않는다"를 분명히 말한다. 다른 조각으로 대신하지 않는다
  - `docs/40_feedback.md` F-05에 같은 두 줄
  - 평가 문항 표현은 넣지 않았다
- **HEAD 기준선을 따로 쟀다** — 15의 1턴 하락(43 → 39)이 누구 몫인지 가르려고, 커밋 `65a3c41`(변경 전) 상태를 임시 worktree에서 두 번 쟀다

| 1턴 46 | HEAD `65a3c41` | 15 | 16 |
|---|---|---|---|
| 둘 다 통과 | 40 · 38 | 39 · 39 | 36 · 40 |
| FEEDBACK 8건 둘 다 통과 | 7 · 7 | 8 · 7 | **8 · 8** |
| F-008 | 실패 · 실패 | 통과 · 실패 | **통과 · 통과** |
| forbid 위반 | 1 · 0 | 0 · 0 | 1 · 1 |

| 여러 턴 12 — 대화 통과 | 15 | 16 |
|---|---|---|
| | 9 · 11 | 11 · 11 |

- **읽는 법**
  - 13-b(43)에서 내려간 몫은 15·16이 아니라 **HEAD 자체**다(40·38). 15·16은 HEAD와 흔들림 범위(±2) 안
  - FEEDBACK 경로만 보면 HEAD 7·7 → 16 8·8. F-008("원래 이래요?" + 문서에 설명 없음)이 두 번 다 풀렸다
  - 16 1회차 36은 Q-003이 FEEDBACK으로 **오분류**된 것 포함(분류 흔들림 — 이 규칙은 분류기가 보지 않는다). 나머지 실패는 HEAD에서도 나오는 U-004·Q-002·Q-004·S-013·S-016·O-007 등
  - forbid 2건(U-001 타임코드 수렴, S-013 읽기 속도)은 USAGE·STYLE_RULE 경로라 이번 변경 밖
- 남은 약점: F-002는 "직접 확인되지 않는다"를 말하면서도 자막 QA 반영 설명 한 줄을 여전히 앞에 붙인다(통과는 함)
- 판정: **유지**
- 로그: `runs/16-answer46*.log` · `runs/16-multiturn*.log` · HEAD 기준선 `runs/16-head-base-answer46*.log`

## 17 분류 지침: 쓴 단어가 아니라 받고 싶은 결과로 판단

- 배경: 분류기는 LLM이지만 경계 규칙이 표면 표현에 기대고 있었다(`'다시 해야 하나요'`, `'원래 이래요?'`, "짜증·불만 표현", "검사 결과를 들고 오면")
  - 실험 13의 오분류 5건이 모두 단어에 끌린 것 — 400002·400018(불만 말투 → FEEDBACK), 400084("읽기 속도" → QC_EXPLAIN), 400105("안 되는 건가요" 말투 → USAGE)
  - 키워드·표현을 더 넣는 방식은 안 된다: 같은 단어가 여러 카테고리에 나온다. 한쪽을 막으면 반대쪽이 샌다(실험 10의 400105가 그 예)
- 바꾼 것
  - `docs/MAPPING.md` 3.0 판단 원칙 신설 — 카테고리별 "받고 싶은 결과" 표, 판정 순서(원하는 결과를 한 문장으로 → 표에서 고름 → 둘이면 경계 규칙)
  - `prompts.py` ROUTE_GUIDE 맨 앞에 옮기고, 경계 규칙을 의도 기준으로 다시 씀
  - `router.py` `reason` 설명: 받고 싶은 결과를 쓰게 함 — 로그에서 판정 근거를 볼 수 있다
  - 평가 문항 문장 누수 0 (극단 단답 "오류"·"디즈니" 2건은 원래 지침에도 있던 단어)
- 결과 (두 번씩): 분류 120 **0.942 · 0.958**, 어려운 72 55 · 61, 1턴 39 · 37, 여러 턴 11
  - 풀림: 400002·400018 두 번 다
  - **새로 샘**: QC_EXPLAIN → STYLE_RULE 5건(400037·400041·400053·400057·Q-007). "검사 이름이 나와도 규정 값을 원하면 STYLE_RULE" 줄이 검사를 돌려 본 결과를 들고 온 문의까지 밀어냈다. 겨냥한 400084는 안 풀림
  - **새로 샘**: F-008 → USAGE. "원래 이래요?"를 "단서일 뿐"으로 낮춰 정상 여부 확인 의도가 약해졌다
- 판정: 두 줄만 고쳐 18로

## 18 17의 과한 두 줄 바로잡기

- 바꾼 것
  - QC_EXPLAIN ↔ STYLE_RULE: **검사를 돌려 본 결과**(뜬 지적·통과·미구현·안 잡힘)를 출발점으로 묻으면 규정 값을 함께 물어도 QC_EXPLAIN. 검사 이야기 없이 쓰는 법만 물으면 STYLE_RULE. 원칙 표의 QC_EXPLAIN 정의도 같게
  - USAGE ↔ FEEDBACK: 동작이 **정상인지 결함인지 확인받고** 싶으면 FEEDBACK — "원래 이래요?"는 그 의도의 흔한 꼴로 명시
- 결과 (두 번씩)

| 평가 | 13 / HEAD | 17 | 18 |
|---|---|---|---|
| 분류 120 정확도 | 0.958 · 0.958 (13) | 0.942 · 0.958 | **0.975** · 0.950 |
| 어려운 72 통과 | 59 (13) | 55 · 61 | **63 · 61** |
| 1턴 46 둘 다 통과 | 40 · 38 (HEAD) · 36 · 40 (16) | 39 · 37 | **42** · 38 |
| 1턴 46 분류 정확도 | — | 0.913 · 0.935 | 0.935 · 0.957 |
| 여러 턴 12 | 11 · 11 (16) | 11 | 11 |
| forbid 위반 | 1 · 1 (16) | 0 · 0 | **0 · 0** |

- QC_EXPLAIN → STYLE_RULE 누수 5건 두 번 다 해소, F-008 복구
- 400002는 1회차 해소 · 2회차 재발(13에선 매번), 400018은 두 번 다 해소
- 여전히: 400084(두 번 다), 400105(두 번 다), 400146(두 번 다 — 13에선 4회 중 1회)
- **새로 샘**: U-004 → QC_EXPLAIN(두 번 다). "효과음 지적이 안 나와요"가 "잡혀야 할 게 안 잡힘 = 검사 결과"로 읽혔다. 정답은 USAGE(종류 설정). U-004는 13부터 답변이 must 누락으로 실패하던 문항이라 통과 수는 그대로다
- 판정: **유지** — 분류·1턴은 흔들림 범위 안에서 같거나 높고, 어려운 문항 +2~4, forbid 0
- 다음 후보
  - "안 잡힘"이 설정 탓인지 검사 결과 탓인지: 검사 설정(종류·플랫폼)을 확인하고 싶은 것이면 USAGE — 원칙 표로 표현할 방법
  - 400105·400084는 의도를 읽어도 남는다. 400105는 문서에 없는 증상인지가 정답을 가른다(분류기가 못 봄), 400084는 "넘치는데"를 검사 결과로 읽는다
- 로그: `runs/17-*.log` · `runs/18-*.log`
